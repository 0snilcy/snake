import React, { FC } from 'react';
import './index.css';

export const GameScreen: FC = () => {
  // <button onClick={() => setGameState(GameState.Action)}>Start</button>
  return <div className="screen"></div>;
};
